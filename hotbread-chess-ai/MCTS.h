#pragma once
#include <map>
#include <string>
#include "State.h"
#include "Game.h"
#include "NeuralNetwork.h"

class MCTS
{
private:/*
	std::map<State, std::map<std::string, float>> q;
	std::map<State, std::map<std::string, float>> p;
	std::map<State, std::map<std::string, unsigned int>> n;
public:
	float search(const State& s, const Game& game, const NeuralNetwork& nnet);*/
};

